import mongoose from "mongoose";
const line = new mongoose.Schema({ variant: { type: mongoose.Schema.Types.ObjectId, ref: "Variant", required: true }, quantity: { type: Number, required: true, min: 1 }, unitCost: { type: Number, required: true, min: 0 }, receivedQuantity: { type: Number, default: 0, min: 0 } }, { _id: false });
const schema = new mongoose.Schema({ supplier: { type: mongoose.Schema.Types.ObjectId, ref: "Supplier", required: true, index: true }, status: { type: String, enum: ["DRAFT", "PENDING_APPROVAL", "APPROVED", "RECEIVING", "CLOSED", "CANCELLED"], default: "DRAFT", index: true }, lines: { type: [line], validate: [(items) => items.length > 0, "Purchase order requires lines"] }, approvedBy: { type: mongoose.Schema.Types.ObjectId, ref: "User", default: null }, evidenceIds: { type: [mongoose.Schema.Types.ObjectId], default: [] }, createdBy: { type: mongoose.Schema.Types.ObjectId, ref: "User", required: true } }, { timestamps: true });
export default mongoose.model("PurchaseOrder", schema);
